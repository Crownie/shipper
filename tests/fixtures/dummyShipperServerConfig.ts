import {SERVER_DIR_MOCK} from '../test-constants';

export const dummyShipperServerConfig = {
  projects: [
    {
      name: 'dummy-project',
      token: 'dummy-token',
      path: SERVER_DIR_MOCK + '/projects/dummy-project',
    },
  ],
};
